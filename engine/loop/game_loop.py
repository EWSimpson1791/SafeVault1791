"""game_loop.py

Top-level game loop placeholder. In the real engine this will orchestrate
setup, turns, and termination conditions. For now it exposes a start()
function that returns immediately.
"""

from typing import Dict, Any

def start(game_state: Dict[str, Any]) -> Dict[str, Any]:
    """
    Start the game loop. Placeholder returns the passed game_state unchanged.
    """
    return game_state
